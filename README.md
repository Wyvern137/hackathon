# Генератор контента для НКО

Telegram-бот для автоматизации создания контента для некоммерческих организаций.

## 📋 Описание

НКО часто не хватает времени или навыков для создания качественного контента. Этот бот помогает генерировать тексты постов, предложения визуалов и идеи контент-плана, обеспечивая лаконичность, читаемость и соответствие стилю НКО.

### Основные возможности

- 🤖 **Генерация текста** — создание постов в свободной форме, структурированной форме или на основе примеров
- 🎨 **Генерация изображений** — создание визуалов для постов с помощью AI
- ✏️ **Редактор текста** — проверка и улучшение текстов с точки зрения грамматики, орфографии и стиля
- 📅 **Контент-план** — автоматическое создание плана публикаций на заданный период
- 📊 **История контента** — сохранение и просмотр всех созданных материалов
- ⚙️ **Профиль НКО** — настройка информации об организации для более релевантного контента

## 🚀 Быстрый старт

### Требования

- Python 3.10+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- API ключ OpenRouter (для текстовой генерации) - [openrouter.ai](https://openrouter.ai)

### Установка

1. Клонировать репозиторий:
```bash
git clone <repository-url>
cd hackathon
```

2. Создать виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установить зависимости:
```bash
pip install -r requirements.txt
```

4. Настроить переменные окружения:
```bash
cp env.example .env
# Отредактируй .env и заполни необходимые ключи
```

5. Создать файл `.env` со следующим содержимым:
```
TELEGRAM_BOT_TOKEN=твой_telegram_bot_token
OPENROUTER_API_KEY=твой_openrouter_api_key
OPENROUTER_MODEL=openai/gpt-4o-mini
DATABASE_URL=sqlite:///bot.db
ENVIRONMENT=development
LOG_LEVEL=INFO
```

### Запуск

```bash
python run.py
```

Или напрямую через модуль:
```bash
python -m bot.main
```

## 📁 Структура проекта

```
hackathon/
├── bot/
│   ├── __init__.py
│   ├── main.py                 # Точка входа бота
│   ├── config.py               # Конфигурация
│   ├── database/
│   │   ├── __init__.py
│   │   ├── models.py           # Модели БД
│   │   └── database.py         # Подключение к БД
│   ├── handlers/
│   │   ├── start.py            # Обработчик /start
│   │   ├── nko_setup.py        # Настройка профиля НКО
│   │   ├── text_generation.py  # Генерация текста
│   │   ├── image_generation.py # Генерация изображений
│   │   ├── text_editor.py      # Редактор текста
│   │   ├── content_plan.py     # Контент-план
│   │   ├── history.py          # История контента
│   │   ├── settings.py         # Настройки
│   │   └── common.py           # Общие обработчики
│   ├── services/
│   │   ├── ai/
│   │   │   ├── openrouter.py   # Интеграция с OpenRouter
│   │   │   └── image_ai.py     # Генерация изображений
│   │   └── content/
│   │       ├── hashtag_generator.py  # Генерация хештегов
│   │       ├── text_processor.py     # Обработка текста
│   │       └── style_checker.py      # Проверка стиля
│   ├── keyboards/
│   │   ├── main_menu.py        # Главное меню
│   │   └── inline.py           # Inline клавиатуры
│   ├── utils/
│   │   ├── validators.py       # Валидация
│   │   ├── formatting.py       # Форматирование
│   │   └── helpers.py          # Вспомогательные функции
│   └── states/
│       └── conversation.py     # FSM состояния
├── data/
│   ├── images/                 # Сгенерированные изображения
│   └── templates/              # Шаблоны
├── tests/                      # Тесты
├── docs/                       # Документация
├── requirements.txt            # Зависимости
├── env.example                 # Пример переменных окружения
├── run.py                      # Скрипт запуска
└── README.md                   # Этот файл
```

## 🛠 Технологии

- **Платформа**: Telegram Bot API
- **Библиотека**: python-telegram-bot 20.7
- **Язык программирования**: Python 3.10+
- **AI для текста**: OpenRouter API (GPT-4o-mini, GPT-3.5-turbo и др.)
- **База данных**: SQLite (для разработки) / PostgreSQL (для продакшена)
- **ORM**: SQLAlchemy 2.0
- **Язык интерфейса**: Русский

## 🎯 Функциональность

### Для пользователя

1. **Запуск бота и приветствие** — понятный интерфейс с описанием возможностей
2. **Сбор информации об НКО** — пошаговый опрос (опционально):
   - Название организации
   - Описание деятельности
   - Типы деятельности
   - Целевая аудитория
   - Стиль изложения
   - Контактная информация
   - Цвета бренда

3. **Генерация контента**:
   - 📝 Тексты постов с хештегами (свободный текст, структурированная форма, на основе примеров)
   - 🎨 Изображения для публикаций (в разработке)
   - ✏️ Редактирование существующих текстов
   - 📅 Создание контент-планов

4. **Дополнительные функции**:
   - 📊 История всех сгенерированных материалов
   - ⚙️ Настройки профиля и уведомлений
   - 💾 Сохранение избранного контента

## 🔧 Конфигурация

Все настройки находятся в файле `.env`. Основные параметры:

- `TELEGRAM_BOT_TOKEN` — токен Telegram бота (обязательно)
- `OPENROUTER_API_KEY` — API ключ OpenRouter (обязательно)
- `OPENROUTER_MODEL` — модель AI для генерации (по умолчанию: `openai/gpt-4o-mini`)
- `DATABASE_URL` — URL базы данных (по умолчанию: `sqlite:///bot.db`)
- `ENVIRONMENT` — окружение (`development` / `production`)
- `LOG_LEVEL` — уровень логирования (`INFO`, `DEBUG`, `ERROR`)

## 📚 Дополнительная информация

- Подробное техническое задание: [docs/Technical_specifications.md](./docs/Technical_specifications.md)
- Руководство по работе с GitHub: [docs/workflow.md](./docs/workflow.md)

## 🚧 Разработка

### Структура БД

Основные модели:
- `User` — пользователи Telegram
- `NKOProfile` — профили НКО
- `ContentHistory` — история сгенерированного контента
- `ContentPlan` — контент-планы
- `PostTemplate` — шаблоны постов
- `NotificationSettings` — настройки уведомлений

### Добавление новых функций

1. Создай обработчик в `bot/handlers/`
2. Добавь клавиатуры в `bot/keyboards/` (если нужно)
3. Реализуй сервисы в `bot/services/` (если нужно)
4. Зарегистрируй обработчик в `bot/main.py`

## 🐛 Известные ограничения

- Генерация изображений временно недоступна (в разработке)
- Структурированная форма генерации текста в разработке
- Генерация на основе примеров частично реализована

## 📝 Лицензия

[Укажите лицензию проекта]

## 👥 Команда

[Укажите участников команды]

## 🤝 Вклад в проект

Предложения и исправления приветствуются! Создайте issue или pull request.
